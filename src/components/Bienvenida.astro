---
const { bienvenida } = Astro.props;
const { titulo, texto } = bienvenida;
---

<div class="flex flex-col place-items-center p-4">
  <h1 class="text-center text-2xl font-semibold">{titulo}</h1>
  <p class="text-sm text-pretty">
    {texto}
  </p>
</div>
