---
const { descripcion } = Astro.props;
const { titulo, subtitulo, lista } = descripcion;
---

<div>
  <div class="contenedor">
    <h1 class="title">{titulo}</h1>
    <p class="subtitle">
      {subtitulo}
    </p>
  </div>
  <ul class="list">
    {
      lista.map((item, index) => (
        <li class="list-item" key={index}>
          {item}
        </li>
      ))
    }
  </ul>
</div>

<style>
  .contenedor {
    display: flex;
    flex-direction: column;
<<<<<<< HEAD
    padding: 1rem;
  }
  .title {
    text-align: start;
    font-size: 24px;
    font-weight: 600;
  }

  .subtitle {
    font-size: 14px;
    text-align: start;
  }

  .list {
    text-align: left;
    padding: 0 16px;
  }

=======
    place-items: center;
    padding: 1rem;
  }
>>>>>>> be43ba3 (Arreglos en JSON y Descripcion)
</style>
