---
const { bienvenidaPrivada } = Astro.props;
const { titulo, texto } = bienvenidaPrivada;
---

<div class="flex flex-col place-items-center p-4">
  <h1 class="text-2xl font-bold text-left mb-2">{titulo}</h1>
  <p set:html={texto} class="text-left text-base" />
</div>
